{
  "name": "claude-code-routines-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Two-pane manager for Claude Code local scheduled tasks and cloud routines",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test \"tests/*.test.js\""
  },
  "devDependencies": {
    "electron": "^43.2.0"
  }
}
